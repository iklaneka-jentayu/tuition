git init
git add .
git commit -m 'init'
git remote add origin https://github.com/iklaneka-jentayu/tuition.git
git branch -M main
git push -f origin main

#-git pull
#git push -u origin main

git add .
git commit -m 'init'
git push -u origin main